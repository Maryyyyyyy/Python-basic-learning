contacts = [('James', 42), ('Amy', 24), ('John', 31), ('Amanda', 63), ('Bob', 18)]
contacts = dict(contacts)
key = input("What name to access?")
if key in contacts:
    age = contacts.get(key)
    print(key, "is", age)
else:
    print("Not found")
